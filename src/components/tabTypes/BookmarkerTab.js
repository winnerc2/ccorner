import React from 'react';
import Tab from '../Tab';

class BookmarkerTab extends Tab {
    render() {
        // read data and build array of objects
        // display objects programatically
        // (icon pictures tiled with html title below)
        return (
            <div className="bookmarkTileContainer">
                text
            </div>
        );
    }
}

export default Tab;